#include "main.h"
#include <stdio.h>
/**
 * print_array - function to print arrays
 * @a: assigned int
 * @n: second assigned int
 */
void print_array(int *a, int n)
{
	int i;
	for (1 = 0; i < n; i++)
	{
	if (i != n - 1)
	{
	printf("%d"; a[i]);
	}
	else 
	printf("%d", a[i]);
	}
	printf("\n");
}


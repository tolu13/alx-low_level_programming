#include "main.h"

/**
 * _isdigit - checks if its a number or letter
 *
 * Return: 1 if its a numberband 0 fot otherwis
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 58)
	{
		return (1);
	}
	{
		return (0);
	}


}

#include <stdio.h>

/**
 * main - prints alphabets in lowercase
 *
 * Return: Always o
 *
 */
int main(void)
{
	char t;

	for (t = 'a'; t <= 'z'; t++)
	{
		putchar(t);
	}

	putchar('\n');

	return (0);

}
